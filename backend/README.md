# TechSwitch Pro - MongoDB Backend API

Node.js Express + Mongoose API for cross-device progress sync.

## 🚀 Local Setup
```bash
cd backend
npm install
npm start
```
Server runs on `http://localhost:5000`.

## 🌐 Deploy to Render / Railway
1. **Root Directory**: `backend`
2. **Build Command**: `npm install`
3. **Start Command**: `npm start`
4. **Environment Variables**:
   - `MONGODB_URI`: `mongodb+srv://ankushform_db_user:FxwrN69M3qBjW7FX@cluster0.kjia8cj.mongodb.net/`
   - `PORT`: `5000` (or dynamic `$PORT`)
